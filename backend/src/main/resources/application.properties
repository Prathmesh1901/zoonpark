# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/parking_booking_db
spring.datasource.username=postgres
spring.datasource.password=Pratham@12
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# JWT Configuration
jwt.secret=mySecretKeyForJWTTokenGenerationParkingBookingApp2026SecureKey123456
jwt.expiration=86400000

# Application Name
spring.application.name=Parking Booking System

# Logging
logging.level.com.parkingbooking=DEBUG
logging.level.org.springframework.security=DEBUG
